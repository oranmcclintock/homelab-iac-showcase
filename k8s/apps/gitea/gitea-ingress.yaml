apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea-ingress
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`gitea.example.com`)
    services:
    - name: gitea
      port: 3000
  tls:
    certResolver: cloudflare
