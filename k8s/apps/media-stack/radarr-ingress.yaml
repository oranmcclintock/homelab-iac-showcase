apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr-ingress
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`radarr.example.com`)
    services:
    - name: radarr
      port: 7878
  tls:
    certResolver: cloudflare
