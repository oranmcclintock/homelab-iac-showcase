apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-ingress
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`sonarr.example.com`)
    services:
    - name: sonarr
      port: 8989
  tls:
    certResolver: cloudflare
